fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
# PyPI did not publish the upstream 1.0.1 release; pin the reviewed source commit instead.
mcp-taiwan-legal-db @ git+https://github.com/lawchat-oss/mcp-taiwan-legal-db.git@f324c00dc87eb49501cf0282433158ddb691f226
# The upstream package currently imports mcp.server.fastmcp, which is removed in MCP 2.x.
mcp>=1,<2
pytest>=8,<10
httpx>=0.27,<1
