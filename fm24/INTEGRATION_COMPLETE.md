# P0／P1／P2 接入完成報告

2026-09-23；基準為 FM24_World_Master_v6.4.0_award_delta_2034_35.final，122 張表、27,124 列。
主檔未修改。原定 8 張 P0 加上 11 張 P1、13 張 P2 均已接入；`PENDING_INTEGRATION` 為空。
P1／P2 合計 707 列，每列全部来源欄位、Excel 列號、來源 ID 與採用限制均保留在 JSON 及讀者頁。

## 讀者入口

| 頁面 | 內容與界線 |
|---|---|
| 國際賽程 | 6 屆賽事、208 筆分組列、141 場賽程；以唯一 Source_ID 連接正式脈絡，保留原始標題、日期範圍、加時／點球註記，不推定晉級隊伍 |
| 俱樂部史料 | 31 筆隊史紀錄、131 筆逐季最佳陣容、12 季皇馬對照、2 筆舊國家德比摘要；各自保留截至日期與統計範圍 |
| 退役檔案 | 3 人的身分快照、生涯摘要、里程碑、完整回顧、11 筆來源問題與 3 筆尚未逐季採用的榮譽敘述；球員頁可連入 |
| 史料與查證 | 24 張 P1／P2 表全部 707 列；切表、搜尋所有欄位／列號、顯示全部、展開原始欄位與來源 |
| 榮譽殿堂 | 3 筆 VDV 正式事實補上當季出場／進球／助攻／評分等明細；多來源不增加得獎次數 |
| 巴薩賽季 | 加入 Barcelona_History 對應來源，延續 P0 的逐欄來源與正式轉會總額 |

P2 的歷史摘要、原始獎項列、匯入決策及擷取問題是查證資料，不重新套用匯入、不展開敘述成獎項、
不把同一得獎者的重複原始列加入正式統計。`COMPLETED_P1_P2` 保存每表的原始限制條件。

## 核對結果

- 皇馬 2034/35 歷史表為季中 29 場／74 分；正式季末榜為 38 場／99 分，快照 2035-06-02。並列保留，不相加、不覆寫。
- 奧斯姆亨退役個人頁為 352 場／216 球，生涯歷史摘要為 384 場／232 球。不同來源範圍分開展示，不覆蓋球員名錄聯賽統計。
- 國家德比保留 44 場舊摘要，最新逐場檔案仍為 46 場。
- 2034 世界盃依正式脈絡顯示日本；原始的 2038 標題及南非 metadata 仍可查閱。
- Master 待核對：2034 世界盃「新西兰」同時出現在第四組與第七組。頁面顯示矛盾與兩筆來源，未自行更正。
- 正式獎項事實仍為 1,132 筆；P1／P2 接入沒有增加獎項次數或改動原有球員生涯總計。

## 實際驗收

完整環境執行 **169 passed in 49.83s，0 skipped**：110 項解析器、24 項 P0、35 項 P1／P2 測試。
新增測試逐表逐列逐欄比對 SQLite 與輸出，並守住歧義關聯、來源範圍、原始註記、分組矛盾及不重複採用契約。
工作簿已於 P0 階段重新匯入 SQLite；本輪使用相同資料庫。

Node 語法檢查通過。瀏覽器 19 個導覽頁均可渲染，验收期間 JavaScript 錯誤與警告皆為 0。
另實際操作最佳陣容賽季切換、VDV 明細、史料搜尋、208 列全部顯示、匯入 JSON 展開及國際賽脈絡警示。
`coverage.py` 宣告與讀取一致，待接入為 0 張／0 列。
用途標籤可重疊；「讀到表」不能證明每欄已採用，亦不能把來源查證列誤稱為正式統計。

## 重建與交付

網站 `archive.html` 含全部展示資料，可直接雙擊離線開啟。
完整包另含程式、測試、`dist/fm24-data.json`、覆蓋率與稽核報告。
若需重新建置，請自行提供同版主檔，執行：

```bash
python -m pip install -r requirements-dev.txt
python etl.py <主檔.xlsx>
python -m pytest test_resolver.py test_evidence.py test_history.py -q
python build_site.py --standalone -o archive.html
python build_site.py --json dist/fm24-data.json
python coverage.py
python audit_brief.py
```

OpenCC 為必要相依套件；缺少時預設拒絕執行。明確降級產物不能與完整環境的數字比較。
Git Credential Manager 已於 2026-09-23 完成帳號授權，解除原先本機 Git 無法推送的阻塞。
發布目標為儲存庫 main 分支的 `fm24/archive.html`；網站位址：
https://kobejordanair-bit.github.io/-/fm24/archive.html 。
