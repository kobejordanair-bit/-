# 完整性修正與剩餘來源限制

2026-09-23。延續 P0／P1／P2 接入，使用同一份 v6.4.0 final 主檔衍生資料庫；122 表、27,124 列。沒有修改來源工作簿或 SQLite 內容。

## 已修正的網站行為

- 積分榜優先取最新正式季末快照。16 組多快照中，只有 5 組明示季中與季末；其他未標註或混合狀態分開顯示。
- 冠軍確認使用主檔受控俱樂部別名與賽季維度，避免巴薩／巴塞羅那等寫法造成假衝突。
- 同名別名對到多個 Club_ID 時不再任選第一個；控制台列出全部完全相同候選。重複身分影響列數不再按同表多個參照欄重複累加。
- 逐季數據只有明示 ADOPTED 才正式採用；未知狀態保留為非採用觀測。
- 轉會方向只按明示轉入／IN、轉出／OUT 分組；其他值獨立展示，不默認為轉出。
- NULL 字串不再變成身分待辦；積分榜 125 筆驚嘆號保留降級語義，均在對應快照末三名。
- 5 筆主辦欄的國家與場地拆開顯示，保留原文。國家／地區身分計數 47，原始 Nation_Dim 49 筆仍完整保留，兩筆聯辦占位不計成國家。
- 完整性頁展示 14 項檢查：8 項網站已處理、5 項需主檔核對、1 項依来源保留未知，附來源與對應頁面入口。
- Data_Issues 全部 366 列與所有來源欄位可查、可搜尋、可按狀態篩選。依主檔明示狀態分類：11 列已處理／確認、330 列來源限制／備註、25 列待核對／尚未修正。未知新狀態預設待核對。這是來源歷史分類，不是宣稱只有 25 個現存資料問題。
- 472 筆未綁定 Player_ID 的正式獎項中，8 筆可找到主檔已接受的唯一別名，提供球員核對連結；不改寫正式事實，不增加得獎次數。

## 尚需主檔證據的五類

| 類別 | 目前範圍與處理 |
|---|---|
| 俱樂部疑似重複身分 | 12 組；先核對主 ID 與轉向關係，不能只按名稱合併 |
| 俱樂部參照未唯一解析 | 81 個字串、993 次參照；同一資料列可以有多個參照。歧義不再被冒認為已解析，所以待辦數增加 |
| 賽事參照未受控 | 61 個字串、2,485 次參照；11 組近似名稱只是候選，級別與年代需查證 |
| 球員獎項身分 | 1,132 筆正式事實中 472 筆缺 Player_ID；包含前述 8 筆唯一別名核對線索 |
| 分組資料矛盾 | 2034 世界盃新西蘭、2026 世界盃伊朗各出現在兩組；雙方原始列均保留並提示 |

2034/35 巴薩 Season Master 的進球／失球依來源政策仍為未知，不拿其他統計範圍覆寫。這項與五類待核對分開計數。

## 驗收

實跑 `pytest test_resolver.py test_evidence.py test_history.py test_integrity.py -q`：**193 passed in 178.69s，0 skipped**。
新增 24 項涵蓋來源 366 列逐欄保留、狀態分類、最新季末排序、冠軍別名對照、同名多 ID、NULL、參照計數、未知轉會方向、主辦場地、國家計數、獎項不重複與缺值不回填。既有 P0／P1／P2 契約測試全部通過。

瀏覽器逐頁驗證全部 19 個入口，無 JavaScript 錯誤；實際操作完整性狀態篩選、來源狀態篩選、問題搜尋、完整來源欄位展開、全部 366 列展示及指定球員連結。JavaScript 語法檢查通過。

產物包括單檔 archive.html、完整 JSON、重新生成的稽核／覆蓋報告，以及附 SHA256 清單的完整交付包。原始缺值與矛盾仍可追溯；本輪完成網站端修正，不宣稱 Master 的所有缺漏都已消失。
